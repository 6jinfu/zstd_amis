# 项目接手规则

- 修改本项目之前，先阅读根目录 `PROJECT_HANDOFF.md`，再按任务定向阅读 `newprd/11_人才发展系统_当前版本PRD.md`；不要从历史 PRD 开始推导需求。
- 用户在当前对话中最新、明确的结论高于已有 PRD、原型和历史资料。
- 产品范围、名称、字段、业务规则、页面结构或关键 UI 结论发生变化时，必须在同一轮修改中同步更新当前 PRD 和 `PROJECT_HANDOFF.md`，并更新交接文档日期。
- `prd/` 和 `newprd/_archive/` 仅用于追溯，不得作为当前功能或页面设计的默认依据。
- 工作区可能包含用户或前序任务的未提交修改；只做当前任务需要的最小改动，不重置或覆盖无关变化。
