# 人才发展原型项目交接

| 项目 | 内容 |
| --- | --- |
| 最后更新 | 2026-09-14 |
| 项目目录 | `/Users/jeff/Documents/Codex/cc_zstd_amis` |
| GitHub | `https://github.com/6jinfu/zstd_amis` |
| 项目形态 | 静态 HTML 高保真业务原型 |

> 后续接手时先读本文，再按任务定向阅读当前 PRD；不要从 `prd/` 或 `newprd/_archive/` 开始通读。

## 1. 需求来源与冲突优先级

1. **当前任务中用户最新、明确的对话结论优先。** 若与已有 PRD、原型或历史资料冲突，以最新对话结论为准。
2. 最新结论必须在同一轮修改中同步到 [`newprd/11_人才发展系统_当前版本PRD.md`](newprd/11_人才发展系统_当前版本PRD.md) 和本文；未同步文档不算完成交接。
3. 对话结论同步后，以当前 PRD 作为完整需求基线，本文只承担快速索引和防回退提醒，不另建平行 PRD。
4. `app/` 用于确认当前已经实现的状态，不能反向推导用户未确认的新需求。
5. [`newprd/10_组织与关键岗位分析_Dify工作流接口.md`](newprd/10_组织与关键岗位分析_Dify工作流接口.md) 只负责组织结构和关键岗位分析的服务端工作流、状态与接口边界。
6. `prd/`、`newprd/_archive/` 仅供追溯；与当前结论冲突时不得采用。

如果本文与当前 PRD 出现冲突，说明文档同步发生漂移：先根据最近一次明确的用户结论修正文档，不要自行选择一个版本继续实现。

## 2. 一分钟了解项目

- 产品名称统一为“人才发展”，当前覆盖岗位、评鉴、发展、人才和系统设置五个中心。
- 项目没有前端构建链和依赖安装，主要由 `app/*.html`、`app/styles.css`、`app/app.js` 和少量页面脚本组成。
- 本地运行使用 `python3 serve.py`；本机访问 `http://127.0.0.1:8766/`，同一局域网的其他电脑使用启动日志显示的 `http://本机局域网IP:8766/`。服务默认监听 `0.0.0.0`，`8766` 是本项目独立使用的默认端口；`serve.py` 对 HTML 禁用缓存、对带 `?v=` 的静态资源启用长期缓存，修改 UI 后必须同步升级版本参数。
- 当前原型强调完整业务流程和统一 UI，不追求后端真实数据；组织结构和关键岗位分析另有 Dify 接口设计。
- 工作区可能存在用户或前序任务尚未提交的修改。开始前先看 `git status --short`，不得重置或覆盖无关改动。

## 3. 当前信息架构

| 一级中心 | 当前业务入口 |
| --- | --- |
| 人才标准 | 组织结构、关键岗位分析、人才标准、指标库 |
| 评鉴中心 | 评鉴工具、人才盘点 |
| 发展中心 | 岗位地图 |
| 人才中心 | 人才列表 |
| 系统设置 | 职级管理、功能设置 |

评鉴中心的层级必须保持：

- 评鉴工具 → 360评估、人才测评、考试。
- 人才盘点 → 宫格盘点、人才图谱盘点。
- 360评估不能与人才盘点放在同一级。
- 不使用“九宫格盘点”作为方法名，因为宫格数量未来不固定。

## 4. 已确认且不可随意回退的产品结论

### 前端公共组件优化（2026-09-08）

- 公共控件 `app/ui/components.js`：对话框、搜索多选、类别管理、学习资源编辑；`standard-ui.js` 仅承接标准预览，跨页不再加载完整标准编辑脚本。
- 标准、评鉴项目、指标库、关键岗位和盘点列表接入公共类别管理；岗位详情和独立学习方案共用资源编辑，添加去重，可移除至空态，选择标准仅预览，保存后生效。
- 列表冻结列改为原始 DOM sticky；表格自身不得 overflow:hidden，否则固定失效。操作文字统一垂直居中。
- 统一资源按钮为添加课程/项目/前测/后测、推荐学习资源；主题白底、主蓝沿用现有设计。
- 最新用户要求：只关注桌面端，节约额度，不继续移动端优化或验收。采用公共组件关键回归、页面加载检查与代表页面视觉抽验，不声称所有页面所有状态均已验收。
- 标准列表与编辑拆为 `standard-list.js` / `standard-editor.js`；公共样式位于 `ui/components.css`。统一品牌图标，补齐原先空图标页面。
- 验收记录见 `deliverables/frontend-review/2026-09-08-review.md`：41 段脚本语法检查、资源引用检查和关键交互通过。当前未提交 GitHub。

### 人才标准与动态应用范围

- 2026-09-10：人才标准成为标准内容与动态应用范围的唯一维护入口，替代岗位中心下并列的岗位管理 / 标准管理入口。标准独立维护，通过指定岗位、人员条件或暂不关联定义应用范围；不新增工作角色库，不恢复旧模型库。
- 页面：`app/standards.html`、`app/standard-detail.html`；共享数据 `standard-seeds.js`、`standards-data.js`，交互 `standard-list.js` / `standard-editor.js`，跨页引用 `standard-references.js`，局部样式 `standards.css`。
- 人才标准列表是唯一标准维护入口；岗位详情只保留适用标准查看、添加关系和跳转上下文，不再维护第二套标准内容。
- 列表仅展示标准名称、适用对象和操作；适用对象直接显示实际岗位名称或条件值，不显示纯数字数量，暂不关联显示“—”。具体应用范围仍在人才标准详情中维护。
- 条件字段只有有来源的部门、现岗位、岗位类别、关键岗位；组内 AND、组间 OR，缺失属性进入待确认。按 5 名实际可用演示人员计算，不编造全量覆盖人数。
- 标准内容继续沿用职责任务、经历经验、能力要求、动力要求、潜力要求五个分项，本轮不实现多等级、复杂认证引擎或客户文件完整导入；芯片、广告仅展示标准示例。编辑器不再提供“引用已有标准”入口，避免回到旧岗位标准复制模型；现有编辑器一个能力组最多 20 项，多组扩展不冒充已实现。
- 人才档案明确选择匹配标准，要求线随选择变化，实际值不变；岗位学习方案保存采用标准并依据能力要求推荐已有资源。
- 标准、类别、关联、采用标准及方案资源使用浏览器本地存储 `talent-standards-v1`，不是后端服务。
- 当前标准链路资源版本为：编辑器 `20260910k`、标准列表 `20260911a`、标准样式 `20260911b`、共享数据 `20260911a`；指标选择器使用 `ui/components.js` / `ui/components.css` `20260910g`、`standard-ui.js` `20260910a` 和共享演示数据 `indicator-data.js` `20260910a`。完整规则及验收见当前 PRD 6.5；指标库能力列表仅展示名称、类别、描述、操作，不回退“分级”“等级数”字段。

### 组织结构与关键岗位分析向导

- 2026-09-14：组织结构分析向导仍保留“组织范围 → 说明材料 → 生成”三步；生成步新增“组织结构矩阵规格”，横轴界面文案为“业务分工”（对应序列数），纵轴界面文案为“业务分权”（对应层级数），均以 1–5 分段控件选择，同步显示实时矩阵预览与宫格总数。选择器、实时预览、矩阵轴标、结果摘要和统计指标必须统一使用“业务分工 / 业务分权”，只在内部字段和业务明细中保留序列、层级概念。默认沿用当前生效版本，首次默认 3×3；提交后规格随任务和版本锁定，结果预览、当前结构及关键岗位引用版本按版本自身规格渲染。空宫格只显示“暂无岗位”，不与矩阵外的待人工归类岗位混淆。对应资源版本为 `styles.css` / `app.js` `20260914d`。
- 2026-09-03：组织结构分析和关键岗位分析统一改为异步后台任务。提交后创建接口立即返回 `202 + run_id`，不得同步等待 Dify；当前页面打开进度弹窗，用户可留在弹窗等待，也可点击“暂时关闭，返回分析页”继续浏览其他页面，不向用户暴露“异步保存”“草稿自动保存”等技术或冗余文案。关闭弹窗后不显示全局浮动提醒，组织结构通过“分析记录”、关键岗位通过项目列表在当前页重开进度弹窗。组织结构默认首次进入无数据，只展示缺省状态及“开始分析”，不提供样例数据捷径；关闭弹窗后回到组织结构主页，任务继续执行。完成并校验通过前不得进入结果页；完成后点击结果按钮先展示装载过渡，再进入对应结果页。用户侧状态统一为草稿、排队中、分析中、校验中、已完成、分析失败、已取消。
- 2026-09-14（二期结论）：组织结构分析最多同时管理 3 个未终态任务。后台任务长时间运行时不自动取消原任务，用户可继续保留原任务并新建分析；新建分析不复制其他任务的组织范围、说明材料或矩阵规格，始终从头重新选择。组织结构主页需保留当前矩阵或空态及“新建分析”，运行任务以任务卡 / 分析记录呈现，不得用 Loading 状态替换整页；达到 3 个任务后阻止第 4 个并提示上限。保持三步向导、进度弹窗、结果校验和人工确认链路不变。
- 2026-08-26 确立三步向导与内置分析参数；2026-09-14 最新结论仅将组织结构矩阵规格开放给用户，分析方法和维度配置仍由 Dify 内置执行。
- 关键岗位分析新建页为**单页表单**（无步骤条、无生成确认汇总）：字段为项目名、类别、分析目标、引用组织结构；底部固定"取消 / 开始分析"。选范围、选视角、视角配置暂不开放用户配置，同样由 Dify 工作流内置默认参数执行。
- 关键岗位分析不新增独立编辑页；项目列表各状态均提供“编辑”操作，在列表内弹窗编辑项目名、类别和分析目标。编辑只更新基本信息，不改变已提交的分析数据、引用组织结构、运行状态或分析结果；类别管理中的类别名编辑仍保留。
- 点击开始分析先进入过渡页 `key-position-analyzing.html`（2026-08-26 新增，分析耗时较长）：卡片样式与组织结构分析过渡卡一致；阶段为 读取组织版本数据 → 经济视角打分 E → 能力视角打分 C → 合成关键系数与理由；卡片元素（项目名、引用版本摘要、双视角口径）必须与表单数据一致，不得虚构。
- 引用组织结构版本为下拉选择，可选全部版本（生效中 + 已存档），选中后表单下方联动展示该版本岗位族矩阵与统计（2026-08-26：从"仅生效中 sel-card"改为 V2/V1 下拉 + 可视化联动；矩阵数据与组织结构页对应版本一致；新建页分节为"基本信息 / 引用组织结构版本"两个独立标题，两卡间距 16px）。
- 2026-09-01：组织结构岗位族矩阵按覆盖人数划分 5 档热力色，依次使用 `#F0F6FF / #DCEAFF / #B8D5FF / #85B7FF / #4D91FF`；人数越多颜色越深，空宫格保持中性空态且不参与计算。矩阵外框和普通宫格分隔线统一使用主蓝 `#0152D9` 的 12% 透明度，避免边线压过热力填充；已调整等业务状态强调边框保持不变。该规则同时适用于当前结构、结果预览和引用组织版本矩阵。
- 被简化步骤的代码不在页面保留（页面注释已标注简化说明，完整向导结构见 git 历史）；`app.js` 向导逻辑保留 `hidden` 步骤自动跳过与重排编号能力，供其他页面或未来恢复使用。恢复完整向导时从 git 历史取回对应代码块。
- 生成步骤的配置汇总不展示内置默认口径：组织结构分析汇总显示组织范围、说明材料、用户选择的矩阵规格和生成物。分析中、结果预览等结果链路页面展示的是分析产出（方法结论、归类依据），不属于配置暴露，保持现状。

### 类别管理

- 盘点项目中的年度盘点、季度盘点、管理者盘点只是项目类别，用于项目管理与筛选，不是盘点范围。
- 关键岗位分析的分析类型也是项目类别。
- 360评估、人才测评、考试均使用同一套类别树、类别管理、新增类别和列表工作区规范。
- 类别树只展示类别名称和交互状态，不在类别名称旁显示项目数、内容数等数字；相关统计仅保留在列表、分页和类别管理抽屉。
- 指标库的能力、动力、潜力是指标类型页签；动力、潜力列表不显示“内置多少条、不分级”等解释行。
- 动力、潜力词条查看抽屉仅保留名称、类别和描述，不显示稳定特质、不分级或测评高低的解释性提示。
- 2026-09-01：指标库能力、动力、潜力的列表样式以岗位管理为唯一基准：工作台撑到视口底部，表格区自适应占满并内部滚动，分页固定在底部；不添加指标库专用线框、固定高度、固定列宽或固定操作列宽。“新建能力”沿用岗位管理的筛选区下方主操作区。业务差异仍保持：能力可维护，动力和潜力为只读固定库。

- 2026-08-26 齿轮图标全站统一到 360 评估基准：类别树面板齿轮（人才测评、考试原为残缺 path，齿轮外圈不闭合）与"新建"旁列表设置按钮（原 ⚙ 字符）均替换为 360 评估同款完整 SVG（feather settings）；新建旁按钮复用 `.assess-action-settings` 类（btn 内 svg 15×15，CDP 实测三页 36×34 按钮盒一致）。

### 360评估

- 新建页当前只保留项目标题、左侧能力指标选择、预览和保存。
- 关联岗位、负责人、被评人员、匹配问卷、评价关系和开始/结束时间所在内容块已按用户要求隐藏，不得依据旧 PRD 擅自恢复。
- 人才测评和考试当前只建设列表与类别管理，不延伸新建、详情、监控或统计下级页面。

### 人才盘点

- 2026-08-31：宫格盘点列表中的“销售铁军盘点”为可点击的已结束案例，通过 `review-detail.html?case=sales` 进入独立销售口径详情；人员部门为销售中心，岗位必须全部为销售相关岗位。
- 宫格盘点创建流程：基本信息、盘点人员、盘点活动、盘点规则设置。
- 盘点活动包含项目数据和绩效数据；项目数据选择已结束且有有效结果的测评项目，绩效模板字段固定为工号、姓名、部门、岗位、绩效等级，等级仅低、中、高。
- 宫格规则当前只使用能力和绩效两个轴，方向可交换；仅能力轴支持设置 `2–5` 档，绩效轴固定沿用上传数据中的低、中、高三档，不提供绩效分档数量修改。宫格总数上限仍为 `25`，当前实际最多 `5×3=15` 格，默认 `3×3`；交换横纵轴时只交换两轴方向，不改变能力轴区间数量及绩效固定三档规则，规格变化会同步更新能力分档、宫格文案预览和结果页链接。能力按得分从高到低做人员排名分档，默认为高档前 `20%`、中档接下来 `20%`、低档其余 `60%`，每档最低 `5%`；调整后自动平衡为 `100%`，分档条和各档控件展示排名区间与预估人数，不额外堆叠 TOP 高档摘要和重复解释文案。宫格文案可编辑。
- 宫格结果页复用组织结构矩阵的查分交互：单元格显示落位与人数，点击后打开右侧人员明细抽屉，人员清单同步按单元格筛选，并可“查看全部”恢复。
- 人才图谱创建流程：基本信息、盘点人员、盘点数据、盘点维度，必须沿用宫格盘点的左侧步骤导航和固定底部操作区。
- 人才图谱数据只能来自人员、绩效、能力、动力、潜力五类现有数据，不增加花名册导入，不编造无来源字段。
- 人才图谱按两个盘点维度形成六类规律分析，使用 ECharts Canvas 展示统计图；不能使用宫格、手画 SVG、CSS 色块或纯文本“规律洞察”代替图表。
- 人才图谱详情只包含基本信息、盘点数据、人才图谱、人才分层；不提供盘点校准、人才池、结果导出或手工结束项目。
- 人才分层只对绩效、能力、动力、潜力的有效评分归一汇总；人员属性只用于分组。当前 A/B/C/D 百分位为 `15% / 25% / 40% / 20%`。

### UI 与页面文案

- 2026-09-04：完成岗位管理页面去模板化收敛。全站品牌入口恢复正式图形标识，不再以“AI”文字作为 Logo；生成类按钮按任务结果命名。岗位管理不额外增加与顶部导航重复的页标题，岗位名作为详情入口并保留编辑、岗位标准、学习方案行操作，关键岗位视图隐藏重复状态列；列表工作台保持统一高度，分页器固定在底部。共享控件提供至少 44px 交互范围，卡片层级改用轻边线、留白和克制阴影表达。
- 2026-09-04：系统设置的职级管理和功能设置由顶部业务导航表达页面身份，内容区不得再增加重复标题；保留必要说明、操作与保存状态。
- 2026-09-04：组织结构与关键岗位的分析中弹窗、结果装载过渡页不显示英文 kicker，直接使用中文业务标题和状态。
- 2026-09-04：修复组织结构当前结果页的依据链路。点击岗位族宫格后，岗位列表操作栏“查看依据”直接打开岗位归类依据抽屉，并展示岗位评价法、结构分析法、建议归属和岗位级说明。
- 延续现有简洁、精致的管理端风格；字号、输入框、卡片间距和按钮间距保持克制一致。
- 2026-09-01：桌面端左侧一级导航支持收起 / 展开；展开为 208px 图标加名称，收起为 72px 纯图标栏，底部按钮负责切换并在同一浏览器跨页面记忆状态。收起后各一级中心图标仍保留 `aria-label` 与 `title`；移动端继续使用顶部横向导航，不显示该按钮。
- 2026-09-01：全站品牌主色改为 `#0152D9`，完整色阶为 `#F0F6FF / #DCEAFF / #B8D5FF / #85B7FF / #4D91FF / #1769F5 / #0152D9 / #0147BE / #063B98 / #07204E`；主按钮悬停使用 `#1769F5`、按下使用 `#0147BE`、禁用使用 `#B8D5FF`。导航、页签、链接、焦点和图表蓝色必须复用该令牌体系，中性画布规则保持不变。
- 全站所有非数据、非状态的结构表面统一使用纯白，包括页面画布、顶部栏、一级导航、内容区、卡片、筛选区、类别树、表格表头和分页；不再使用浅灰或浅蓝大面积底色。层级通过轻边线、留白和必要阴影表达；品牌按钮、选中态、状态标签、图表与热力单元格保留业务颜色。
- 详情页保留左侧页内导航，不擅自改成横向页签；导航统一为透明浅色形态（152px、无底色块、active 字重 600 + 左侧 2px 主色条，同 4219 段 review-detail-nav 规格，2026-08-26 起岗位详情 position-detail 与人才档案 talent-profile 一并纳入，消除灰底块差异）。
- 类别树、列表筛选、表格、分页和固定操作列应复用统一 UI。
- 列表主操作按钮位于筛选区下方左上角（2026-08-26：组织结构页“新建分析/历史分析/了解分析方法”从右上角移到左上角对齐全站规范）；指标库能力页同样遵守，不得再将“新建能力”并入筛选工具栏。
- 图表和结果页不提供导出按钮；导出只保留在列表页（2026-08-26：key-position-result、review-detail、talent-profile 已删导出按钮）。
- 2026-09-02：岗位管理新增“销售经理（SA-201）”关键岗位演示案例；列表的编辑、岗位标准和学习方案入口均带销售案例参数，详情页的岗位信息、AI 岗位标准、能力选择、已有学习资源和 AI 配课全程使用销售口径，不能复用算法工程师内容。
- 六个类别树面板页（360 评估、人才测评、考试、人才盘点、关键岗位分析、指标库）统一规格：管理类别齿轮 32×32、类别标题 13px/600（2026-08-26 统一；此前人才盘点/关键岗位/指标库为 28px）。
- 2026-08-31：评鉴工具三个列表页的类别树全面对齐人才盘点基准：左栏 208px、内边距 14px 12px、标题区和类别项均为 40px，标题区不带横线，工作区最小高度 668px；禁止再以评鉴工具专用规则覆盖该基准。
- 筛选条件只能引用列表数据真实存在的维度，选项值与列表值域一致（见 PRD BR-08；2026-08-26 已清理“所在组”、人才测评“项目来源”、人才列表“部门/标签”等冗余条件，考试列表岗位值换为在册岗位）。
- 二级页返回链接统一为 svg 尖括号 + "返回"形态（2026-08-26：宫格盘点详情、人才图谱详情的"← 返回"文字箭头已改，与岗位详情/360 详情/学习方案一致）；详情区节标题（sect-title）不带下边框横线（2026-08-26 去除）。
- 全站横向二级 tab 统一为 46px 容器 / 44px tab / 13px / 500 字重 / 28px 间距 / 1px 底部绘制区 / 距内容 14px；普通文字 `--neutral-600`、选中文字 `--primary-700`，容器使用 1px `--border-subtle` 底线，选中项统一用贴线的 2px `--primary-600` 伪元素。页面级、卡片内和岗位地图序列 tab 使用同一套规则，选中态不得改变字重；可滚动容器不得裁掉高亮线底部 1px。
- 同源页面切换不得出现旧版导航骨架或整页白闪：共享 Shell 必须在浏览器首次绘制前同步初始化；Chrome 使用 140ms 纯透明度跨页面过渡，不使用位移或长动画，并尊重 `prefers-reduced-motion`。
- 页面只展示完成任务需要的字段、状态、结果口径和操作后果。
- 不在页面展示 PRD 注解、未来规划、点击教学、快照、接口、存储、算法过程、数据模型或“用户不需要理解”的解释句。
- 图表编码和结果口径等必要信息可以保留；不要把所有辅助文字机械删除。
- 静态资源引用带版本参数（当前 `styles.css?v=20260903k`、`app.js?v=20260903k`），每次部署同步更新版本号，根治浏览器缓存导致的“假性不一致”（2026-08-26 已两次踩坑：图表消失、tab 位置错觉）。

## 5. 常用页面入口

| 任务 | 页面 |
| --- | --- |
| 组织结构 | `app/org-structure.html`、`app/org-analyze.html`、`app/org-preview.html` |
| 关键岗位分析 | `app/key-position.html`、`app/key-position-new.html`、`app/key-position-analyzing.html`、`app/key-position-result.html` |
| 人才标准与应用范围 | `app/standards.html`、`app/standard-detail.html` |
| 岗位与指标 | `app/positions.html`、`app/position-detail.html`、`app/vocabulary.html` |
| 评鉴工具列表 | `app/assess-projects.html`、`app/talent-assessment-projects.html`、`app/exam-projects.html` |
| 360评估 | `app/assess-new.html`、`app/assess-detail.html` |
| 人才盘点列表 | `app/review-projects.html` |
| 宫格盘点 | `app/review-new.html`、`app/review-detail.html` |
| 人才图谱盘点 | `app/review-map-new.html`、`app/review-map-detail.html`、`app/review-map-charts.js` |
| 发展与人才 | `app/position-map.html`、`app/learning-plan.html`、`app/talent-list.html`、`app/talent-profile.html` |

完整有效页面清单仍以当前 PRD 第 18 节为准。

## 6. 修改与验证方式

1. 先读本文和任务涉及的当前 PRD 小节，再读对应页面代码；不要先读历史 PRD。
2. 只修改任务所需文件，保留工作区内无关变化。
3. UI 修改后使用 `python3 serve.py` 运行，并在真实页面检查目标视口、交互、溢出和控制台错误。
4. 至少执行 `git diff --check`；提交前再核对正式导航和重定向页面。
5. 产品范围、名称、字段、业务规则、页面结构或关键 UI 结论变化时：
   - 更新当前 PRD 对应章节；
   - 更新本文“已确认结论”、页面入口或待办；
   - 更新本文顶部日期；
   - 如涉及组织结构或关键岗位后端契约，再更新 10 号接口文档。
6. 旧结论失效时直接修订当前文档，不新增“新版 PRD”“最终版 PRD”等平行文件；需要追溯的旧文档放入 `_archive/`。

## 7. 快速回归清单

- 导航名称和层级是否符合第 3 节。
- 类别与组织范围、分析维度是否被错误混用。
- 页面是否出现用户未确认的新字段、新方法或延期功能。
- 页面是否出现内部实现、未来规划或多余教学文案。
- 列表是否具备查询、类别树、主操作、统一表格和固定分页。
- 人才图谱是否由本地 ECharts 渲染，图表间距和容器高度是否正常。
- 向导底部操作区是否固定，详情页左侧导航是否保留。
- 浏览器是否无横向溢出和控制台错误。
- Chrome 中切换一级中心、业务菜单和详情页时是否无旧 Shell 跳变、整页白闪；第二次访问同版本页面时 CSS/JS 是否命中缓存。

## 8. 当前明确不做

- 独立首页、独立 AI 对话入口。
- 人才测评和考试的新建、编辑、详情、监控、统计页面。
- 考试题库和人才发展考试组卷。
- 独立测评结果、独立人才池、继任地图、组织看板、职级地图、个人发展计划。
- 人才图谱中的宫格、校准、人才池、导出结果和手工结束项目。

## 9. 最近同步记录

- 2026-09-14：根据最新口径，绩效数据上传继续固定低 / 中 / 高三档；宫格规则仅开放能力轴 `2–5` 档设置，绩效轴不再提供分档数量修改，结果页按固定三档渲染。
- 2026-09-14：全站类别树移除类别名称旁的数字，覆盖评鉴工具、人才盘点、关键岗位分析、人才标准、指标库及通用列表侧栏；类别管理抽屉和数据列表中的统计数字保留。
- 2026-09-14：创建宫格盘点规则页做减法，移除 TOP 高档摘要卡、步骤副标题及重复的灰色解释文案；保留分档条、排名区间、预估人数和合计等直接决策信息，整体保持规则与数据口径不变。
- 2026-09-14：组织结构分析开放横轴 1–5 项业务分工、纵轴 1–5 级业务分权的自定义矩阵规格；采用分段选择而非自由数字输入，并将选择器、实时预览、矩阵轴标、结果摘要和统计指标统一为“业务分工 / 业务分权”，提供总宫格数反馈、任务锁定和按版本规格渲染。
- 2026-09-14：宫格盘点规则页支持横轴与纵轴分别选择 `2–5` 档，限制宫格总数最多 `25`，默认保留 `3×3`；结果页按规格动态渲染矩阵，并新增点击宫格打开人员明细抽屉、同步筛选人员清单的查分交互。
- 2026-09-10：统一指标库能力、动力、潜力类别树的视觉与筛选交互规范；能力树保留层级缩进和展开/收起，但与动力、潜力统一行高、选中态、悬停态、计数位置和焦点样式；所有类别节点不显示文件夹/文件图标，只保留可展开节点的箭头，搜索提示“搜索能力指标类别”放入搜索框内；修复能力树叶节点被通用 `.empty` 样式误命中导致行高异常的问题，并支持根节点展开/收起。

- 2026-09-10：能力、动力、潜力选择统一改为全屏三栏穿梭框；能力指标库补齐按截图口径的技能 / 知识 / 素养 / 新品导入与开发层级演示树，能力列表移除“分级”“等级数”字段；修复能力列表工作台横向滚动容器被 `overflow:hidden` 截断的问题。指标选择器与共享演示数据资源版本为 `components 20260910f`、`standard-ui 20260910a`、`indicator-data 20260910a`。

- 2026-09-10：按最新产品结论将一级中心和主入口统一命名为“人才标准”；移除标准管理并列入口，人才标准列表收敛为标准名称、适用对象数量和操作三列，具体岗位与动态条件仅在详情应用范围中维护；同步升级资源版本至 `20260910b`。
- 2026-09-10：复核标准维护链路，移除人才标准详情中的重复“对象名称/对象说明”和“引用已有标准”入口；岗位详情仅负责关联、查看和下游采用已有标准，标准名称、应用范围与标准内容统一由人才标准入口维护；编辑器资源版本升至 `20260910h`，共享数据资源升至 `20260910b`；应用范围编辑不再展示冗余的可用人数、匹配人数和匹配人员预览摘要；返回/取消主动放弃草稿，不再被未保存拦截误伤。
- 2026-09-10：标准详情左侧导航将基本信息、应用范围及“职责任务、经历经验、能力要求、动力要求、潜力要求”七个入口统一平铺，不再使用“标准内容”父级层级；右侧仍一次只编辑一个内容分项；编辑器资源版本升至 `20260910j`，标准样式资源保持 `20260910c`。
- 2026-09-10：人才标准类别演示数据调整为通用标准、岗位序列标准（产品序列、研发序列、销售序列、专业序列）、职级标准、专项标准，并在列表左侧按树形层级展示；标准样式升至 `20260910d`，标准列表升至 `20260910b`，共享数据升至 `20260910d`。
- 2026-09-10：类别树移除类别文字前的特殊字符，层级仅通过缩进和纯 CSS 展开箭头表达；标准样式升至 `20260910e`，标准列表升至 `20260910c`。
- 2026-09-10：人才标准类别树移除类别图标，不可展开节点预留等宽对齐位，只有父节点保留必要的展开箭头；类别点击只筛选，父节点箭头只展开/收起，键盘触发重绘后保持焦点。标准样式升至 `20260910l`，标准列表升至 `20260910g`。
- 2026-09-10：加大岗位序列标准子类别的左侧缩进，强化父子层级关系；标准样式升至 `20260910f`。
- 2026-09-10：人才标准详情页改为主体撑满可用高度，保存/取消操作区贴近页面底部；长内容仍保持滚动，操作区增加底部安全区；标准样式升至 `20260910g`。
- 2026-09-10：详情内容区补充与底部操作区等高的安全留白，确保长内容滚动到末尾时不被保存/取消区遮挡；标准样式升至 `20260910h`。
- 2026-09-03：两条耗时分析链路改为异步后台任务；提交后在当前页打开可关闭进度弹窗，关闭后可继续浏览且不显示全局浮动提醒，分析记录/项目列表可重开进度弹窗；组织结构后台等待态增加岗位族矩阵构建动画和分阶段状态反馈；完成前禁用结果入口，完成后点击结果按钮先展示精简装载过渡；同步补齐 `202 + run_id`、后台 Worker、轮询与终态接口约束。
- 2026-09-02：清理延期功能的不可达实现；8 个旧页面收敛为轻量兼容跳转，人才详情源码只保留“综合能力 / 盘点结果”，同步删除共享脚本中的旧页重定向、IDP 列表配置和未使用 MOCK 数据。
- 2026-09-02：补齐销售岗位建模与配课演示链路；岗位管理新增销售经理，岗位详情按案例切换销售职责、经历、4D 指标、可选能力、培训课程、培养项目及前后测，保留高级算法工程师原演示链路。
- 2026-09-01：纠正指标库列表的错误页面特例，改为以岗位管理列表为唯一基准；删除指标库专用 523px 高度、固定列宽、112px 操作列与线框感，恢复表格区自适应撑满、内部滚动、分页置底，并将“新建能力”恢复到筛选区下方主操作区。
- 2026-09-01：按 UI review 减少组织结构页重复线条；矩阵去掉外框与蓝色宫格线，改用 4px 留白分隔，移除矩阵标题底线、统计区横线和 KPI 四边框，保留顶部栏、左导航、控件与业务状态的必要边界。
- 2026-09-01：本地预览服务由仅监听 `127.0.0.1` 改为监听 `0.0.0.0`，启动日志显示当前局域网访问地址，支持同一局域网内其他电脑访问。
- 2026-09-01：全站非数据、非状态区域改为纯白背景；统一画布、顶部栏、一级导航、内容区、卡片、筛选区、类别树、表头与分页的白色表面，灰蓝色仅退出结构背景，不影响品牌交互色、状态色、图表和热力数据色。
- 2026-09-01：组织结构页非数据区域统一为白色；画布、顶部栏、一级导航、操作区、矩阵外层、行列标题、空宫格和统计卡片去除浅灰 / 浅蓝底，仅岗位族单元格保留 5 档热力色。
- 2026-09-01：关键岗位分析列表页去除浅灰、浅蓝大面积背景，画布、顶部栏、一级导航、类别树、筛选区、表格与分页统一为白色，通过轻边线、留白和主蓝选中态优化层级。
- 2026-09-01：减弱组织结构热力宫格边缘线条；外框与普通分隔线由完整 `#B8D5FF` 改为 `#0152D9` 的 12% 透明度，保留已调整宫格的状态强调边框。
- 2026-09-01：新增全站左侧一级导航收起 / 展开能力；收起态只保留图标并同步扩展顶部栏和内容区，状态通过本地存储跨页面保持。
- 2026-09-01：组织结构分析热力宫格由“单一旧蓝色透明度”改为品牌色阶 5 档离散映射（`#F0F6FF` 至 `#4D91FF`），增强不同覆盖人数宫格的辨识度。
- 2026-09-01：读取并落地新的 10 档品牌蓝色系；全站主色统一为 `#0152D9`，补齐默认、悬停、按下和禁用角色令牌，同时将人才档案、组织看板和人才盘点详情中的旧蓝色透明填充改为引用全局品牌色。
- 2026-08-31：修复评鉴工具与人才盘点类别树不一致；360 评估、人才测评、考试的左栏位置、宽度、内边距、标题区、横线和类别项尺寸均对齐人才盘点。
- 2026-08-31：宫格盘点“分档规则”升级为按能力排名的可调占比控件；能力三档支持数字输入与 `±5%` 步进，其他两档自动平衡，界面同步展示“高档选取前百分之几”、排名带、各档排名区间与预估人数。
- 2026-08-31：将“销售铁军盘点”补齐为可点击的已结束宫格盘点案例，详情中项目、绩效、人员、校准和人才池数据统一使用销售业务口径。
- 2026-08-31：关键岗位分析列表曾移除项目“编辑”入口；2026-09-14 起恢复为列表内基本信息编辑，不新增独立编辑页。弹窗仅编辑项目名、类别和分析目标，保存后同步更新列表与类别筛选，不改变分析数据、引用组织结构、运行状态或结果。
- 2026-08-26：明确“最新对话结论优先”，建立交接文档与强制同步机制。
- 2026-08-26：评鉴工具拆分为 360评估、人才测评、考试；人才盘点拆分为宫格盘点、人才图谱盘点。
- 2026-08-26：补齐人才图谱创建、六类统计图和人才分层；图表统一使用本地 ECharts。
- 2026-08-26：清理页面中的路线图、内部实现、点击教学和重复解释文案。
- 2026-08-26：修复长页面滚动后背景变蓝问题——移除 `html` 浅蓝底与 `body` 蓝渐变，画布统一中性浅灰（`app/styles.css` 第 31 节），人才图谱盘点等页面验证通过。
- 2026-08-26：简化两个分析向导的操作流程——组织结构分析保留三步步骤条（组织范围→说明材料→生成），关键岗位分析新建页为单页表单（无步骤条），被简化步骤的参数由 Dify 工作流内置默认，PRD 6.1/6.2 已同步。
- 2026-08-26：规律分析条件联动——人才图谱六类分析按部门/岗位范围与维度对切换重渲染图表、统计标注与样本量（PRD 7.3 已同步）。
- 2026-08-26：数据与筛选一致性清理——删除“所在组”筛选与列（岗位管理/添加关键岗位抽屉/配置地图/关键岗位清单）、人才测评“项目来源”筛选与列、人才列表“部门/标签”幽灵条件并补查询脚本；考试列表岗位值换为在册岗位；新增 PRD BR-08。
- 2026-08-26：六页类别树面板齿轮统一 32×32、标题 13px/600；组织结构页操作按钮移到左上角；图表与结果页删除导出按钮；全站静态资源加版本参数 `?v=20260826b`。
- 2026-08-26：修复长页滚动后背景色差带——body 实际生效第 36 节 `background:var(--canvas)`，html 误用 `--neutral-50` 与之相差一档；html 统一改用 `--canvas` 并删除基础段 body 冗余背景声明。
- 2026-08-26：修复 tab 下划线视觉变细——`.tab-btn` 的 `margin-bottom:-1px` 被 `.tab-btns` 的 `overflow-x:auto` 裁掉 1px；card-tabs-solid 模式下 tab-btns 嵌 card-head、高度由内容撑起时触发，补 `padding-bottom:1px`（key-position-result 验证，全站 9 页复测无遮挡）。
- 2026-08-26：新增关键岗位分析过渡页 `key-position-analyzing.html`（PRD 6.2 已同步）；版本参数升级 `?v=20260826c`。
- 2026-08-26：新建关键岗位分析"引用组织结构版本"改为下拉可选全部版本（V2 生效中 / V1 已存档），选中后表单下方联动展示对应版本岗位族矩阵 + 统计（V2 3×3 / 34 岗 165 人，V1 3×2 / 30 岗 141 人，热度自动着色）。
- 2026-08-26：新建页重构为两个分节标题——上"基本信息"（项目名/类别/分析目标）、下"引用组织结构版本"（版本下拉 + 摘要 + 岗位族矩阵，头部右侧显示选中版本名与状态标签）；修复两卡间距为 0 的问题（补 mt-md，16px）。
- 2026-08-26：详情页左侧导航统一——岗位详情/人才档案的 `.layout-detail .anchor-nav` 灰底块（surface-subtle）与人才盘点详情不一致，扩展 4219 段 review-detail-nav 五条选择器命中 `data-page=positions/talent-list`，四详情页（position-detail/talent-profile/review-map-detail/review-detail）实测形态完全一致；版本参数升至 `?v=20260826d`。
- 2026-08-26：去掉详情区节标题横线（`.detail-main .sect-title` 的 border-bottom，岗位标准/人才档案同治）；二级页"← 返回"文字箭头统一为 svg 尖括号版（review-detail、review-map-detail）；版本参数升至 `?v=20260826e`。
- 2026-08-26：指标库页面右侧"透明遮罩"根因修复——(1) 抽屉关闭态（translateX(100%) 堆在视口右缘外）box-shadow 渗入页面形成灰蓝竖带，阴影规则从 `.drawer` 收口到 `.drawer.show`（styles.css 三处：基础段/1652 段/1932 段）；(2) vocabulary 工作台复用 `100dvh-118` 高度公式但外层多包 card-body，产生 30px 死滚动使滚动条常驻，改 `body[data-page=vocabulary]` 高度 auto，scrollHeight 1013→989 零溢出。
- 2026-08-26：全站二级 tab 几何统一——`.tab-btns`（全局段 46 容器/44 tab/13px/2px 线/`--border-subtle`/mb14）、`.assessment-tool-tabs`（线色对齐变量、gap 30→28、mb 16→14）、`.pm-tabs`/`.lm-tabs`（岗位地图、职级地图由胶囊式改文字底线式，level-map 内嵌样式移除、容器改 `.lm-tabs` 类）、`card-tabs-solid` 变体 card-head 补 1px 底线 + tab 44；CDP 实测 9 页容器 46/线 1px 同色/下划线 2px。版本参数 `?v=20260826g`。
- 2026-08-26：当前目录作为独立新项目运行，项目路径更新为 `/Users/jeff/Documents/Codex/cc_zstd_amis`，本地预览默认端口由旧项目使用的 `8765` 调整为 `8766`；2026-09-01 为支持局域网访问，监听地址改为 `0.0.0.0`。
- 2026-08-26：二级 tab 全面复查并二次收口——修复指标库/人才盘点被高优先级规则放大左右 padding 与 tab 高度、评鉴工具使用伪元素而其他页使用 border、岗位地图 active 字重 600、创建/详情页 margin 18/20 等差异；页面级与内容级 tab 统一到同一几何、颜色和 2px 伪元素横线。浏览器实测 7 个当前有效交互场景，控制台无错误；静态资源版本升至 `?v=20260826h`。
- 2026-08-26：修复二级 tab 高亮线实际像素不一致——`.tab-btns` 的 `overflow-x:auto` 会连带使纵向溢出受裁切，`bottom:-1px` 的 2px 伪元素因此只显示 1px，而 `.assessment-tool-tabs` 的 `overflow:visible` 显示完整 2px；所有 tab 容器统一增加 1px 底部绘制区，人才盘点与 360 评估实测均完整显示 2px。当前静态资源版本为 `?v=20260826j`。
- 2026-08-26：修复 Chrome 跨页面功能切换时整页闪烁——共享脚本不再等待 `DOMContentLoaded` 后重建导航，而是在底部脚本执行时同步完成 Shell 与列表骨架初始化；同源页面启用 140ms 纯透明度 View Transition，减少动态效果偏好下自动关闭；开发服务器改为 HTML 不缓存、带 `?v=` 静态资源长期缓存，避免每次跳转重复下载 CSS/JS。版本参数升至 `?v=20260826j`。
